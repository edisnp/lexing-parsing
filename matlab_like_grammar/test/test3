function Z = interpolation(varArgument)

for i = length(varArgIn):-1:1
    x(i) = [0, 1, -2]
    error('There should exist at least one interpolation node')
end

for i = 0:1:10 % This is another comment
    y(i) = []
end
